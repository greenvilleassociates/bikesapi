using dirtbike.api.Data;
using dirtbike.api.Models;
namespace Enterpriseservices
{

	public class ParkUtils
	{
/*
			public void updatevisitors(string somepark, int someadults, int somekids)
			{
 					using (var context = new DirtbikeContext())
            		{
					ParkCalendar[] someCalendar = context.ParkCalendars.Where(m => m.ParkId == somepark).ToArray();
                			context.ParkCalendars.Attach(someCalendar[0]);
                			someCalendar.Visitors  = someCalendar.Visitors + (someadults + somekids);
					await context.SaveChangesAsync();
                	return;
 
   					}
			}

			public void setmaxvisitors(string somepark, int somemaxvisitors)
			{
					using (var context = new DirtbikeContext())
            		{
					Park[] somePark = context.Parks.Where(m => m.ParkId == somepark).ToArray();
		                	context.Cards.Attach(somePark[0]);
                			somePark[0].Maxvisitors  = somemaxvisitors;
					await context.SaveChangesAsync();
                	return;
 
   					}
			}*/

	}
}
